enum ShowcaseShape { rectangle, circle, roundedRectangle, custom }

enum TooltipPosition {
  top,
  bottom,
  left,
  right,
  auto,
  topLeft,
  topRight,
  bottomLeft,
  bottomRight,
}

enum ShowcaseAnimation { fade, scale, slide, bounce, elastic, none }

enum HighlightBehavior { pulse, glow, breathe, static, ripple, shimmer }

enum AutoPlayMode { none, sequential, smart }
